## 图表清单

### 第1章 前言（0 张）
纯文字叙述，不需配图。

### 第2章 主要任务（1 张）

| 编号 | 图题 | 类型 | 内容说明 |
|------|------|------|----------|
| 图2-1 | Agent 长时记忆场景下的 I/O 瓶颈示意 | 场景示意图 | Agent 交互产生记忆 → 向量嵌入 → 写入外存索引。图遍历检索产生随机读 → OS Page Cache 失效 + 顺序预取无用。红叉标记失效的 OS 机制 |

### 第3章 总体设计（4 张）

| 编号 | 图题 | 类型 | 内容说明 |
|------|------|------|----------|
| 图3-1 | 系统分层架构图 | 架构框图 | 五层结构：CLI → 检索引擎 → I/O 调度(缓存/预取/LSM) → I/O 后端抽象(SyncIO/CachedIO/UringIO) → 磁盘格式 |
| 图3-2 | 读路径数据流图 | 时序图 | 查询 → Beam Search → Cache.Get → (miss) ReadAt 读盘 → PutIfRoom 回填 → 距离计算 → TopK |
| 图3-3 | 写路径数据流图 | 时序图 | 插入 → MemTable.Add → 缓冲满 → BuildKnnGraph → WriteSegment → Manifest.Save。虚线标注后台 Compaction |
| 图3-4 | .vsg 段文件二进制布局 | 数据结构图 | 4096B 头部 → data_offset 起数据记录区(dim×float+degree×uint32) → 可选 PQ 区(码本+codes)。标注 4KB 对齐边界 |

### 第4章 详细设计（8 张）

| 编号 | 图题 | 类型 | 内容说明 |
|------|------|------|----------|
| 图4-1 | Vamana 图构建算法流程 | 流程图 | 输入 → 随机初始化 → Medoid 选择 → for 每节点: GreedySearch → RobustPrune → 对称化 → 超度回剪。标注 R/α/beam_width/max_visits |
| 图4-2 | Beam Search 搜索流程 | 流程图 | 入口节点 → while frontier 非空: pop 最近 → 遍历邻居 → PQ? ADC:L2 → 加入 frontier → 截断 beam_width → 提交预取 |
| 图4-3 | LSM-Tree 分层合并示意 | 状态转换图 | MemTable(内存) → flush → Level-0(≤4个) → 合并 → Level-1(≤2个) → 合并 → Level-2 |
| 图4-4 | PQ 压缩与 ADC 距离计算 | 原理图 | 128 维 → M=16 子空间(各 8 维) → 查最近聚类中心(K=256) → 16B codes。查询侧: ADC 表(M×K) → Σ table[i][code[i]] |

### 第5章 遇到的问题（2 张）

纯文字叙述，不需配图。

### 第6章 实验与性能评估（5 张）【我自己写】

| 编号 | 图题 | 类型 | 内容说明 |
|------|------|------|----------|
| 图6-1 | 搜索参数网格扫描热度图 | 热力图 | X=max_visits, Y=beam, 单元格=Recall@10+颜色深浅。标注默认参数点和 Pareto 前沿 |
| 图6-2 | 消融实验柱状图 | 分组柱状图 | A/B/C/D 四种配置耗时柱状图，分段标注磁盘 I/O vs CPU，Recall 叠加折线 |
| 图6-3 | 缓存命中率-容量曲线 | 折线图 | X=缓存大小(0/2/20/80/200MB), Y 左=命中率, Y 右=耗时，数据点标注具体值 |
| 图6-4 | 多线程加速比曲线 | 折线图 | 实测加速比 vs 理想线性(虚线)，4-8 线程区间标注 SSD 带宽瓶颈 |
| 图6-5 | 读写混合负载延迟分布 | 箱线图 | 四种负载的 P50/P95/P99 箱线，标注与纯读基线偏移 |



