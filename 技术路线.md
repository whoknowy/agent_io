量化类方案（Faiss、IVFOADC+G+P 等）：通过向量压缩实现低内存占用，可支撑单节点十亿级数据，但损失 y 压缩导致召回率极低，1-recall@1 普遍不足 60%；
图类方案（HNSW、NSG 等）：高召回、低延迟，但必须全内存存储索引与全量向量，单节点支撑十亿级数据需要 TB 级内存，只能通过多机分片部署，成本极高。
业界普遍认为，将索引放在 SSD 上会导致查询延迟与吞吐量的灾难性下降。
这是一个非常扎实且具有工业挑战性的系统级任务，核心是**在“内存受限”和“动态读写混合”的双重约束下，打通向量检索算法与底层存储I/O的壁垒**。建议你按照「**理论调研→架构设计→MVP实现→模块迭代→压测调优**」的路径逐步推进，以下是具体的入手指南和核心拆解：

---

### 第一步：先做“定向调研”，明确技术坐标系
不要急于写代码，先用1-2天时间把以下核心问题搞清楚，避免走弯路：
1.  **向量索引与I/O的绑定关系**：
    - 选一个**适合做I/O优化和动态更新**的基础向量索引作为载体（推荐二选一：① **IVF系列**（量化索引，天然分桶，I/O可控）；② **Vamana/HNSW**（图索引，拓扑连通性适合做预取，但动态更新需设计））。
    - 精读你提供的两篇论文中关于“存储格式”和“I/O路径”的部分：Milvus的LSM-based动态管理、DiskANN的SSD存储布局与BeamSearch。
2.  **底层I/O技术的速成**：
    - 快速学习Linux `io_uring` 的核心概念（SQE/CQE、队列深度、非阻塞I/O），找一个简单的“异步读文件”示例跑通（推荐用C++的 `liburing` 库）。
    - 复习LSM-Tree的核心思想：MemTable→Immutable MemTable→SSTable→分层合并，推荐看LevelDB的极简架构文档。
3.  **明确“红线指标”的拆解**：
    - 内存10%-20%：意味着**全量向量数据必须在磁盘**，内存只放缓存、索引元数据（如IVF的聚类中心、图的起始节点）和压缩向量（可选，如PQ压缩）。
    - Recall@10≥85%：这是算法参数与I/O优化的平衡器——不能为了减少I/O而过度牺牲召回（比如IVF的`nprobe`不能太小，图索引的`efSearch`不能太小）。

---

### 第二步：设计“最小可行架构（MVP）”，先跑通闭环
建议先搭建一个**不带复杂优化但能跑通“存→查→写”**的原型，再逐步叠加任务要求的三个优化方向。推荐的分层架构如下：

```
┌─────────────────────────────────────────────────────────┐
│  向量检索引擎层（适配IVF/Vamana，提供Top-K接口）        │
├─────────────────────────────────────────────────────────┤
│  【任务核心】I/O调度与存储引擎层                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  用户态缓存  │  │  拓扑预取器  │  │  LSM写管理器  │ │
│  │  池（淘汰）  │  │  (io_uring)  │  │  (MemTable)  │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
├─────────────────────────────────────────────────────────┤
│  存储格式层（磁盘上的向量/索引布局，扇区对齐）          │
└─────────────────────────────────────────────────────────┘
```

**MVP的实现优先级（按顺序）**：
1.  **先搞定“存储格式层”**：
    - 设计一个简单的磁盘文件格式：比如分为「元数据区」（存向量维度、数量、索引参数）、「向量数据区」（连续存储原始向量，每个向量偏移量可计算，无需额外索引）、「索引结构区」（如果是IVF，存聚类中心和每个桶的向量ID列表；如果是图，存每个节点的邻居ID列表）。
    - 关键：**所有数据对齐SSD页大小（通常4KB）**，避免跨页I/O。
2.  **再实现“基础读路径”**：
    - 不做缓存和预取，先实现“按需从磁盘读向量/索引节点”的基础逻辑，能跑通Top-K查询，用小数据集（如SIFT1M）验证Recall@10达标。
3.  **最后叠加“基础写路径”**：
    - 用最简单的LSM思想：新写入的向量先放内存（比如一个`std::vector`），查询时先查内存再查磁盘；内存满了就直接追加写到磁盘的一个新“段”，暂时不做后台合并（先保证写能进去，读能读到）。

---

### 第三步：逐个攻克任务要求的三大优化模块
这是参赛的核心得分点，每个模块都有明确的“设计抓手”：

#### 模块1：用户态I/O缓存池（读优化）
**核心目标**：替代OS Page Cache，针对向量检索的“热点访问”特征设计。
- **缓存什么？（缓存对象设计）**
  - 优先级1：**索引元数据**（IVF的聚类中心、图索引的起始节点/高层节点、高频访问的枢纽节点）——这些数据体积小但访问频率极高。
  - 优先级2：**最近访问过的向量数据和索引节点**——利用向量检索的“局部性”（比如图搜索会反复访问某些路径，IVF搜索会集中访问某些桶）。
- **缓存淘汰策略（核心创新点）**：
  - 不要只用LRU！建议设计**“加权淘汰策略”**：
    - 给每个缓存条目打标签：`is_metadata`（元数据权重最高，尽量不淘汰）、`access_count`（访问频率）、`node_importance`（如果是图节点，可根据出度或在搜索中的被访问概率打分）。
    - 淘汰时优先淘汰权重最低的冷数据。
- **内存控制**：
  - 实现简单的**内存水位线**：当缓存占用达到数据集大小的15%（取10%-20%的中间值）时，触发淘汰，回落到12%左右。

#### 模块2：向量检索感知的I/O预取（读优化）
**核心目标**：利用索引拓扑，用`io_uring`把“磁盘读延迟”隐藏在“CPU计算距离”的时间里。
- **预取的触发时机（与检索算法深度绑定）**：
  - 如果你选**IVF索引**：当开始计算查询向量与聚类中心的距离时，预取“Top-nprobe个最相似聚类中心”对应的桶数据。
  - 如果你选**图索引（推荐Vamana/HNSW）**：这是最能出亮点的场景！
    - 实现**Beam Search（束搜索）** 而不是单纯的贪心搜索：在搜索的每一步，维护一个“候选节点集”。
    - 当CPU正在计算“当前候选节点集”与查询向量的距离时，用`io_uring`异步预取这些候选节点的**所有邻居节点**的数据。
- **io_uring的使用要点**：
  - 用**SQPOLL模式**（如果内核支持）减少系统调用开销；
  - 设置合理的**队列深度**（比如16-64），避免I/O拥塞；
  - 实现一个简单的“预取调度器”：只预取“未来1-2步大概率会访问的数据”，不要预取太多浪费带宽。

#### 模块3：LSM-Tree实时写优化（写优化）
**核心目标**：把随机写变成顺序写，同时保证读延迟不抖动。
- **MemTable的设计**：
  - 内存中的MemTable不仅要存新插入的向量，还要**维护一个小型的内存索引**（比如IVF的话，新向量也做一次轻量聚类放到内存桶里；图的话，新节点先在内存里建临时边），保证查询MemTable时也能高效。
- **段（Segment）的设计**：
  - 磁盘上的每个段都是一个“小的完整索引+向量数据”，段内的数据是不可变的。
- **后台合并（Compaction）的设计**：
  - 这是平衡“写放大”和“读放大”的关键：
    - 实现**分层合并策略**（类似LevelDB）：Level 0是刚刷盘的小段，Level 1是合并后的大段，以此类推。
    - 合并时**重建索引而不是简单追加**：比如合并两个IVF段时，重新做一次聚类；合并两个图段时，重新计算边的连接（这部分可以简化，比如只保证段内的图连通，跨段的边通过缓存弥补）。
  - **流量控制**：当有大量写入时，降低合并速度；当系统空闲时，加快合并，避免读延迟抖动。

---

### 第四步：技术选型与工具推荐
- **编程语言**：**C++**（唯一选择，需要裸机I/O控制、内存管理和极致性能）。
- **核心库**：
  - `liburing`：io_uring的用户态封装库。
  - `FAISS`：可以借用它的向量距离计算函数（如内积、欧氏距离）和基础索引结构（如IVF的聚类），但I/O路径要完全自己写。
  - `nlohmann/json`：用于存元数据的序列化（可选）。
- **测试数据集**：
  - 先用**SIFT1M/SIFT10M**（小，调试快），再用**SIFT1B/DEEP1B**的子集（验证内存受限场景）。
- **压测工具**：
  - 自己写一个简单的多线程压测器：模拟“N个线程做Top-K查询，M个线程做插入”的混合负载，统计QPS、P95/P99延迟、Recall@10。

---

### 最后的“避坑”提醒
1.  **不要一开始就追求“大而全”**：比如先实现IVF索引的I/O优化，比一上来就搞复杂的图索引更容易出成果；
2.  **Recall是底线**：所有I/O优化都不能以牺牲召回为代价——比如预取失败了没关系，大不了再同步读，但不能为了减少I/O而减少`nprobe`或`efSearch`；
3.  **重视“单节点调试”**：先用gdb或perf看“一次查询到底读了多少次磁盘”、“缓存命中率是多少”、“io_uring的等待时间占比”，再针对性优化，比盲目改代码有效。

如果你需要某个具体模块（比如io_uring的预取逻辑、LSM的合并策略）的更深入设计细节，可以随时告诉我！