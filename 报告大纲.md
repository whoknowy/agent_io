# 报告大纲：面向 Agent 长时记忆的高性能 I/O 优化向量检索存储引擎

---

## 摘要

- **背景**：大模型 Agent 的长时记忆场景需要大规模高维向量检索，内存受限（10%-20% 数据集大小）下传统 OS I/O 机制失效
- **核心问题**：图索引随机读使 Page Cache 失效、实时随机写造成 SSD 写放大
- **解决方案**：设计 vindex——基于 Vamana 图索引的外存向量检索引擎，实现三大 I/O 优化
- **关键指标**：SIFT-1M 数据集，Recall@10 = 0.9897，内存占用可控，支持读写混合负载

---

## 第1章 前言

### 1.1 课题背景
- LLM Agent 长期记忆的存储需求：大规模、高维、动态
- 向量检索在 Agent 记忆中的应用场景（RAG、上下文回忆）
- 内存墙问题：全内存方案成本极高，外存方案面临 I/O 瓶颈

### 1.2 国内外研究现状
- 量化类方案（Faiss IVFOPQ）：低内存但召回率低
- 图类方案（HNSW/NSG）：高召回但全内存，单机 TB 级内存
- DiskANN：将 Vamana 图索引放 SSD，全量数据驻留磁盘
- Milvus：LSM-Tree 风格段管理

### 1.3 本文主要工作
- 实现 Vamana 图索引的外存存储与检索
- 用户态缓存池替换 OS Page Cache（加权淘汰）
- 拓扑感知异步预取（利用图结构隐藏 I/O 延迟）
- LSM-Tree 写入优化（顺序追加写 + 分层合并）
- 乘积量化（PQ）压缩降低存储和 I/O 开销

### 1.4 论文结构
- 第二章：主要任务分析
- 第三章：总体架构设计
- 第四章：各模块详细设计
- 第五章：关键问题与解决方案
- 总结与展望

---

## 第2章 主要任务

### 2.1 赛题要求分析
- 内存约束：数据集大小的 10%-20%
- 召回约束：Recall@10 ≥ 85%
- 读优化：用户态缓存 + 拓扑感知预取
- 写优化：LSM-Tree 随机写转顺序写
- 透明集成：作为底层存储引擎提供标准接口

### 2.2 任务分解
- **存储格式层**：磁盘段文件（.vsg）格式设计，4KB 扇区对齐
- **读路径**：Beam Search + 缓存池 + 异步预取
- **写路径**：MemTable → Segment Flush → Compaction
- **压缩层**：乘积量化（PQ）编码/解码与 ADC 距离计算

### 2.3 技术选型
- **索引算法**：Vamana (DiskANN) — 适合外存布局，拓扑可预取
- **编程语言**：C++17 — 裸机 I/O 控制、零拷贝内存管理
- **I/O 框架**：同步 fstream（跨平台）+ Linux io_uring（高性能后端）
- **测试数据集**：SIFT-small（1 万）、SIFT-1M（100 万）
- **评价指标**：Recall@10、QPS、查询延迟、缓存命中率

---

## 第3章 总体设计

### 3.1 系统架构

```
┌─────────────────────────────────────────────────────────┐
│  CLI 层 (main.cpp)                                       │
│  build | query | eval | insert | train-pq               │
├─────────────────────────────────────────────────────────┤
│  向量检索引擎层                                           │
│  ┌─────────────────┐  ┌──────────────┐                  │
│  │  Vamana 图构建  │  │ Beam Search  │                  │
│  │  (graph_builder)│  │(graph_search)│                  │
│  └─────────────────┘  └──────────────┘                  │
├─────────────────────────────────────────────────────────┤
│  I/O 调度与存储引擎层                                     │
│  ┌───────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │ 用户态    │  │ 拓扑预取器   │  │ LSM 写管理器    │  │
│  │ 缓存池    │  │(prefetch_    │  │ (MemTable +     │  │
│  │(cache_pool)│  │ scheduler)   │  │  Compaction)    │  │
│  └───────────┘  └──────────────┘  └─────────────────┘  │
├─────────────────────────────────────────────────────────┤
│  I/O 后端抽象层 (io_backend)                              │
│  ┌───────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │ SyncIO    │  │ CachedIO     │  │ UringIO         │  │
│  │ (fstream) │  │ (装饰器)     │  │ (io_uring/Linux)│  │
│  └───────────┘  └──────────────┘  └─────────────────┘  │
├─────────────────────────────────────────────────────────┤
│  磁盘存储格式层 (.vsg 段文件 + manifest.txt 清单)        │
└─────────────────────────────────────────────────────────┘
```

### 3.2 数据流设计
- **读路径**：查询 → Beam Search → Cache Lookup → (miss) Disk Read → (hit) 内存返回
- **写路径**：新向量 → MemTable → Flush to Level-0 Segment → Compaction Merge
- **预取路径**：Beam Search 候选集 → SubmitPrefetch(邻居节点) → 后台 io_uring → TryGet

### 3.3 模块划分
- `core/`：基础类型（VectorId, SearchParams, SearchResult）、距离计算（L2Squared）、Top-K 收集器
- `index/`：图构建器（精确/Vamana）、图搜索器（Beam Search + 预取）
- `io/`：I/O 后端抽象、同步后端、缓存装饰器、预取调度器、io_uring 后端
- `cache/`：用户态缓存池（加权淘汰）
- `storage/`：段文件读写、清单管理、LSM 分层合并
- `pq/`：乘积量化训练（K-Means）、编码/解码、ADC 距离
- `dataset/`：SIFT 数据集加载器（fvecs/ivecs/bvecs）
- `mem/`：MemTable 写缓冲
- `util/`：CLI 参数解析、线程池

### 3.4 磁盘文件格式
- **段文件 (.vsg)**：4096 字节头部（魔数 VSG1、版本、维度、数量、度、入口、记录大小）+ 数据区（每个节点 dim×float + degree×uint32）+ PQ 区（码本 + 编码）
- **清单文件 (manifest.txt)**：`path|id_offset|level`，管理多段
- **码本文件 (.pcb)**：魔数 VPQC + M/K/subspace_dim + 质心数据
- **数据集配置 (datasets.json)**：数据集名称 → 文件路径映射

---

## 第4章 详细设计

### 4.1 整体思路

本系统的核心矛盾在于：Vamana 图索引在 Beam Search 过程中产生的访问模式是极度随机的——每一步扩展当前节点的邻居时，下一批待访问节点的 ID 完全由图的拓扑结构决定，与存储顺序无关。这种"图遍历驱动的随机读"使得操作系统默认的 Page Cache（基于 LRU + 顺序预取）几乎完全失效：预取器猜不中下一次读的位置，Page Cache 被大量一次性访问的数据污染后频繁换入换出。同时，Agent 记忆场景下的实时写入直接修改磁盘上的图结构将导致灾难性的随机写放大。

基于以上分析，本系统的设计思路是：**不依赖 OS 的通用 I/O 机制，在用户态构建一套完全面向向量检索访问特征的 I/O 子系统**。具体而言——

- **读路径**：用 Beam Search 的图遍历过程本身作为"预取线索"。搜索每向前推进一步，候选集（frontier）中排名靠前的节点就是未来 1-2 步最可能访问的目标。利用后台线程在这些节点的数据尚未被需要时提前发起异步读取，将磁盘延迟隐藏在 CPU 计算当前节点距离的时间窗口内。同时，引入一个带加权淘汰策略的用户态缓存池，优先保留图枢纽节点和索引元数据。

- **写路径**：借鉴 LSM-Tree 的思想，新写入的向量不直接修改磁盘上的已有段，而是在内存中缓冲，攒够一批后整体构建为新的不可变段文件追加写入磁盘。后台合并线程在系统空闲时将多个小段合并为大段，同时重建图索引以保证连通性。

- **压缩路径**：用乘积量化（Product Quantization）将 128 维浮点向量压缩为 16 字节的短码。搜索时先通过 O(M) 的表查找（ADC 距离）快速筛选候选节点，仅对最终候选者做精确 L2 校验，大幅降低需要从磁盘读取的原始向量数据量。

以下各节按模块逐一展开上述设计的实现细节。

---

### 4.2 图索引的外存构建与搜索

#### 4.2.1 Vamana 图构建

Vamana 算法（源自 DiskANN）的核心思想是构建一个满足 α-RNG 剪枝准则的有向近似 KNN 图，使得贪心搜索在有限的步数内以高概率收敛到查询向量的真实最近邻。

构建流程如下：首先为每个节点随机分配 R 个邻居完成图初始化（R 即为目标出度，默认 32）。然后通过 100 次随机采样选取 Medoid（到其他节点平均距离最小的节点）作为图的全局入口。接下来进入迭代改进阶段——对每个节点，以当前图结构运行 GreedySearch，找到 beam_width 个候选邻居，再通过 RobustPrune 按 α-RNG 准则筛选：对于候选 v，如果存在已选邻居 u 使得 α·dist(p,v) > dist(v,u)，则 v 被 u "遮挡"而无需加入。α 越大，剪枝越激进，图越稀疏。最后将筛选出的有向边对称化为无向边，对超度节点再次剪枝。

本系统在基础 Vamana 上做了两方面适配：一是将 GreedySearch 的每轮线性扫描前沿集优化为最小堆维护（节点入堆时计算并缓存距离，pop_heap 取最小），构建效率提升超过 100 倍；二是支持暴力/Vamana/auto 三种构建模式的自动调度——当数据集规模不超过 10000 时使用精确暴力建图，超过时自动切换 Vamana。

#### 4.2.2 Beam Search 搜索

查询时采用 Beam Search 而非朴素贪心搜索。搜索从图的全局入口（Medoid）开始，维护一个按距离排序的最小堆候选集（frontier），每轮弹出最近未访问节点，遍历其所有邻居计算距离后加入候选集，将候选集截断至 beam_width。这一过程重复直到候选集为空或访问节点数达到 max_visits。

Beam Search 的关键优势在于：beam_width 控制搜索的"宽度"（探索性），max_visits 控制"深度"（计算量上限）。增大 beam 可以降低陷入局部最优的概率，但计算开销线性增长；增大 max_visits 直接提高召回但延长搜索路径。本系统默认取 beam=8、max_visits=1000，在 SIFT-1M 上 Recall@10 达 0.9897。

#### 4.2.3 外存适配

每个节点的数据（128 个 float 向量 + 32 个 uint32 邻居 ID）约 640 字节，1M 节点总计约 640MB。如此大的数据量不可能常驻于受限内存。本系统将所有向量数据和图结构存储于磁盘段文件（.vsg）中，节点按 ID 顺序排列，偏移量可通过 `id × record_size` 直接计算。搜索时按需通过 `SegmentReader::ReadNode()` 随机读取特定节点的数据。

---

### 4.3 用户态 I/O 缓存池

#### 4.3.1 设计动机

OS Page Cache 面向通用 workload 设计，其核心假设是"时间局部性"和"顺序访问"。但在图索引的 Beam Search 中，节点的访问顺序由图的拓扑决定而非存储顺序——每次搜索路径不同，访问的节点在磁盘上分散分布。这导致 OS 的 LRU 淘汰和顺序预取策略完全失效：预取器每次都猜错，Page Cache 中被读入的冷数据占据空间后被整体换出，命中率极低。

本系统在用户态自行管理一块固定大小的内存作为缓存池，绕过 OS Page Cache，直接通过 `std::fstream` 按偏移量读取（绕过缓冲区的 seek + read）。

#### 4.3.2 缓存条目分类与加权淘汰

缓存池中的每个条目被赋予一个权重，公式为：

```
weight = priority × 1000 + access_count
```

条目分为三个优先级：

- **Priority 3（元数据）**：段头部（4096 字节，含维度、数量、PQ 参数等）和索引元数据。这些数据仅占缓存极小比例，但每次搜索都要读取，标记为 "几乎不可淘汰"。
- **Priority 2（热节点）**：access_count ≥ 5 的节点。图搜索中的枢纽节点（出度高、被多条路径穿越）会被反复访问，自然积累高访问计数。
- **Priority 1（冷节点）**：普通数据节点。大部分节点在搜索中仅被访问一两次。

淘汰时扫描整个缓存 map 找最低 weight 条目逐出。access_count 只升不降——这适配了 Agent 记忆场景下同一批长期记忆被反复查询的负载特征。

#### 4.3.3 内存控制

缓存池通过 `watermark_bytes` 控制内存上限。写入操作区分两种策略：`Put` 触发驱逐以确保写入成功（用于元数据和关键数据），`PutIfRoom` 则在空间不足时直接跳过（用于普通节点数据）。经实测，将 `CachedIOBackend::ReadAt` 的缓存回填从 `Put` 切换为 `PutIfRoom`（缓存满后不驱逐冷数据、直接跳过插入），避免了每次 cache miss 触发全表扫描驱逐的 O(N²) 性能灾难。

在 200MB 缓存限制下，SIFT-1M 的 10K 查询全量测试取得 49.8% 的缓存命中率。

#### 4.3.4 与 I/O 后端的集成

`CachedIOBackend` 采用装饰器模式，实现了与底层 `SyncIOBackend` 完全一致的 `IIOBackend` 接口，使得上层代码无需感知缓存的存在。`ReadAt` 的调用路径为：先查缓存（`cache_->Get`），命中则直接返回；未命中则委托底层后端读取磁盘，读完后通过 `PutIfRoom` 尝试写回缓存。

---

### 4.4 拓扑感知异步 I/O 预取

#### 4.4.1 核心思路

光有缓存还不够——缓存只能加速"已经访问过"的节点，对于首次访问的节点仍需要同步等待磁盘 I/O。本系统的核心创新在于利用 Beam Search 的图遍历过程本身作为预取的"前瞻窗口"。

如图 4-6 所示，Beam Search 每轮从 frontier 中选择最近节点进行扩展。扩展完成后，frontier 中的节点就是未来几步最可能访问的目标。本系统在此时立即提交这些节点的异步预取请求，后台预取线程在 CPU 计算当前节点距离的同时从磁盘拉取数据。等到下一轮需要这些节点的数据时，有较大概率数据已经准备就绪。

#### 4.4.2 PrefetchScheduler 实现

预取调度器维护一个后台工作线程，通过生产者-消费者模型运行：

- `SubmitPrefetch(path, offset, size)`：将预取请求加入队列，返回 request_id。若当前在途请求数已达 max_in_flight（= beam_width），直接返回 0 表示拒绝，防止 I/O 拥塞。
- `TryGet(request_id, &buf)`：非阻塞查询——若数据已就绪则取出并返回 true；否则立即返回 false，调用方回退到同步读取。
- `WaitGet(request_id, &buf)`：阻塞等待版本，用于关键路径。

一个重要的工程优化是文件句柄缓存：预取线程通过 `unordered_map<path, ifstream>` 维护已打开的文件句柄，避免每次读取都走 `open`/`close` 系统调用路径（在 Windows 上尤其昂贵）。

#### 4.4.3 与 GraphSearcher 的集成

`GraphSearcher` 在搜索循环中维护一个 `prefetch_requests[node_id]` 映射表，记录每个节点是否已发起预取。每轮扩展节点后，遍历 frontier 的前 beam_width 个节点，对尚未发起预取的节点调用 `SubmitPrefetch`。当后续某轮需要读取这些节点时，先调用 `TryGet` 尝试命中预取缓存；若命中，直接解析预取缓冲区中的二进制数据（通过 `SegmentReader::ParseNode`），省去一次磁盘读取。

---

### 4.5 LSM-Tree 写入优化

#### 4.5.1 问题分析

如果每次插入一条新向量都直接修改磁盘上的图索引文件，将产生以下问题：(1) 图结构修改需要更新该节点及其所有邻居的数据记录，单次插入触发多次随机写；(2) 段的物理布局被破坏，节点的固定记录大小假设不再成立，偏移量无法直接计算。借鉴 LSM-Tree 的设计哲学，本系统将写入操作转换为对磁盘的顺序追加写。

#### 4.5.2 MemTable 与批量刷盘

新插入的向量首先在内存的 MemTable 中累积。MemTable 本质上是一个 `std::vector<float>`，同时维护一个小型的内存 KNN 索引。当缓冲向量数达到 `flush` 阈值（或输入数据全部加载完毕），对整批向量调用 `BuildKnnGraph` 构建一个完整的图，将该批数据一次性写入一个新的 Level-0 段文件。这一批量建图策略将"逐条插入修改磁盘"的 N 次随机写转换为 1 次顺序追加写。

#### 4.5.3 分层合并策略

随着写入持续，磁盘上积累了大量的小段文件，导致查询时需要在每个段上独立搜索后合并结果（读放大）。CompactionScheduler 在独立工作线程中运行，采用分层合并策略控制段的数量：

- **Level 0**：MemTable 直接刷盘的段，最多允许 4 个。一旦达到阈值，将所有 Level-0 段合并重建为一个 Level-1 段。
- **Level N（N≥1）**：合并产生的段，每层最多 2 个。达到阈值则合并为该层级的下一层。

合并过程为：读取源段的所有向量数据 → 全部合并为一个向量集 → 重新运行 `BuildKnnGraph` 重建图索引 → 写入新的段文件 → 通过原子替换更新 manifest → 删除旧段文件。

#### 4.5.4 写入一致性保证

manifest 的更新采用"写临时文件 + 原子替换"策略：先将新内容写入 `manifest.txt.tmp`，然后通过 `MoveFileExW`（Windows）或 `rename`（Linux）原子替换原文件。任何时刻系统崩溃，要么看到旧版本 manifest，要么看到新版本，不会出现中间态。

---

### 4.6 乘积量化压缩

#### 4.6.1 压缩原理

乘积量化（Product Quantization, PQ）是一种有损向量压缩技术。其核心思想是将 d 维向量空间分解为 M 个互不相交的低维子空间（每个子空间维度为 d_sub = d/M），在每个子空间中独立运行 K-Means 聚类（K 个聚类中心），用聚类中心的 ID 替代原始子向量。压缩后每个向量仅需存储 M 个 uint8 编码（每个 1 字节），总计 M 字节，相比原始 d×4 字节大幅压缩。

以 SIFT-1M（d=128）为例，取 M=16, K=256，每个向量压缩为 16 字节，压缩比 = 512/16 = 32:1。

#### 4.6.2 ADC 距离计算

搜索时使用非对称距离计算（Asymmetric Distance Computation, ADC）：查询向量不做压缩，仅数据库向量压缩。首先构建查询向量到所有聚类中心的距离查找表（`BuildADCTable`，大小为 M×K），然后对任意压缩向量，其与查询向量的近似距离为：

```
ADC(q, code) = Σ_{i=0}^{M-1} table[i][code[i]]
```

此计算仅需 M 次表查找和 M-1 次加法，时间复杂度 O(M)，远低于精确 L2 的 O(d)。在 Beam Search 中，大量邻居节点的距离估算通过 ADC 快速完成，仅对最终进入 Top-K 的候选节点做精确 L2 校验。

#### 4.6.3 码本训练

码本通过 K-Means 训练获得。使用 SIFT 提供的 learn 向量集（100K 条），对每个子空间独立运行 25 轮迭代。训练好的码本连同配置（M, K, d_sub）写入 .pcb 文件，构建索引时加载并用于编码所有数据库向量。

---

### 4.7 并发查询支持

面对多线程并发查询的场景，系统需要保证 IO 子系统的线程安全性。`ThreadPool` 基于模板化的任务队列实现，工作线程从共享队列中取任务执行。

核心挑战在于：每个 `SegmentReader` 内部持有 `unique_ptr<IIOBackend>`（含一个 `std::fstream` 文件句柄）。`std::fstream` 的 `seekg` + `read` 不是线程安全的——多线程共享同一个文件句柄时，线程 A 的 `seekg` 可能在线程 B 的 `read` 之前被修改，导致读到错误位置的数据。实测中 4 线程共享 SegmentReader 时 Recall 从 0.994 暴跌至 0.28。

解决方案：每个工作线程创建独立的 `SegmentReader` 实例。主线程仅从已加载的段中提取元数据（文件路径 + ID 偏移量），工作线程内部使用这些元数据自行打开文件、构建独立的 IO 后端。`CachePool` 使用 `std::shared_mutex` 实现读写锁，允许多个线程同时读取缓存，仅在写入时独占。

---

### 4.8 关键数据结构与接口

#### 4.8.1 段文件二进制格式

.vsg 文件布局如图 3-4 所示。4096 字节的固定头部包含魔数 "VSG1"、版本号、向量维度、节点数量、图出度、全局入口节点 ID、单条记录大小、数据区起始偏移，以及可选的 PQ 参数（M、K）。数据区从 4096 字节偏移处开始，每条记录紧凑排列：前 dim×4 字节为原始浮点向量，后 degree×4 字节为邻居 ID 数组。若启用了 PQ，则在数据区之后追加码本（M×K×d_sub 个 float）和全部节点的压缩编码（count×M 字节）。

#### 4.8.2 核心接口

```cpp
// I/O 后端抽象
struct IIOBackend {
    virtual bool Open(const std::string& path) = 0;
    virtual bool ReadAt(uint64_t offset, void* buf, size_t size) = 0;
    virtual bool WriteAt(uint64_t offset, const void* buf, size_t size) = 0;
};

// 图搜索器
class GraphSearcher {
    std::vector<SearchResult> Search(
        const float* query, uint32_t dim,
        const SearchParams& params, SearchStats* stats);
    void SetPrefetchScheduler(PrefetchScheduler* prefetch);
};

// 预取调度器
class PrefetchScheduler {
    uint64_t SubmitPrefetch(const std::string& path, uint64_t offset, size_t size);
    bool TryGet(uint64_t request_id, std::vector<uint8_t>& out);
};

// 缓存池
class CachePool {
    bool Get(const std::string& path, uint64_t offset, size_t size, void* out);
    void PutIfRoom(const std::string& path, uint64_t offset, const void* data, size_t size);
};
```

### 4.9 本章小结

本章从"图遍历随机读使 OS I/O 失效"这一核心矛盾出发，逐一阐述了本系统在用户态实现的三项 I/O 优化机制。加权缓存淘汰策略将缓存命中率提升至约 50%，拓扑感知预取利用搜索过程本身作为前瞻窗口隐藏磁盘延迟，LSM-Tree 写优化将随机写转化为顺序追加写。乘积量化压缩进一步将单节点数据量从 640 字节压缩至约 20 字节（原始向量 512B 压缩为 16B codes + 邻接表 128B），使得同等内存下可缓存更多节点。各模块通过抽象的 I/O 后端接口解耦，可独立替换（如同步后端 vs io_uring 后端），具备良好的可扩展性。

---

## 第5章 遇到的问题与解决方法

### 5.1 Vamana 构建性能问题
- **现象**：1 万向量 Vamana 构建跑不完，比暴力还慢
- **根因**：GreedySearch 每轮迭代对所有 frontier 节点重算 L2Squared 距离（O(N×max_visits×frontier_size)），而非缓存复用
- **解决**：最小堆代替每轮线性扫描，节点进入 frontier 时计算并缓存距离。20K 从"跑不完"降到 1 分 20 秒，优化幅度 >100×

### 5.2 多线程竞态问题
- **现象**：4 线程 eval 时 Recall 从 0.994 降到 0.28
- **根因**：多个线程共享同一个 `SegmentReader`（含 `unique_ptr<IIOBackend>`），底层 `std::fstream` 的 seek/read 不是线程安全的
- **解决**：每个线程创建独立的 `SegmentReader` + `IIOBackend` 实例。`seg_infos` 仅保存元数据（路径+offset），线程内各自打开文件

---

## 第6章 实验与性能评估

### 6.1 实验环境与数据集

#### 6.1.1 硬件环境
- CPU：Intel Core i5-13500hx（10 核 16 线程，基础频率 2.6GHz，睿频 4.7GHz）
- 内存：16GB DDR5-4800
- 存储：NVMe SSD 1TB
- 操作系统：Ubuntu 24.04.1 LTS (WSL) /Windows 11 

#### 6.1.2 软件环境
- 编译器：GCC  C++17，-O2 优化
- 构建系统：CMake 3.16+，Ninja 生成器

#### 6.1.3 测试数据集

| 数据集 | 向量数 | 维度 | 原始大小 | 用途 |
|--------|--------|------|----------|------|
| SIFT-small | 10,000 | 128 | ~5 MB | 功能验证、快速调试 |
| SIFT-1M | 1,000,000 | 128 | ~512 MB | 主测试集、全量 benchmark |
| SIFT-1B query  | 10,000 | 128 | ~1.3 MB | 写入负载测试（uint8 格式） |

#### 6.1.4 索引构建参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 图出度 (R) | 32 | Vamana 最大出度 |
| 构建束宽 (build-beam) | 64 | 构建时 GreedySearch 束宽 |
| 剪枝系数 (α) | 1.2 | RobustPrune α-RNG 参数 |
| PQ 子空间数 (M) | 16 | SIFT-1M 用；siftsmall 用 64 |
| PQ 聚类数 (K) | 256 | 每个子空间 256 个聚类中心 |
| 训练向量数 | 100,000 | PQ 码本训练样本量 |

### 6.2 搜索参数敏感性分析

#### 6.2.1 实验设计
在 SIFT-1M 全量索引（100 万向量，PQ M=16）上，对搜索的两个核心参数 beam（束宽）和 max_visits（最大访问节点数）进行全因子网格扫描。beam 取 {4, 8, 16, 32}，max_visits 取 {500, 1000, 2000, 5000}，共 16 组参数组合。每组使用前 200 条查询评估 Recall@10。

#### 6.2.2 实验结果

| beam | max-visits=500 | 1000 | 2000 | 5000 |
|------|---------------|------|------|------|
| 4 | 0.9695 | 0.9895 | 0.9940 | 0.9955 |
| 8 | 0.9695 | 0.9920 | 0.9950 | 0.9970 |
| 16 | 0.9730 | 0.9905 | 0.9960 | 0.9975 |
| 32 | 0.9765 | 0.9915 | 0.9960 | 0.9975 |

#### 6.2.3 结果分析
beam 对召回的影响远小于 max_visits——beam 从 4 增大到 32 仅带来约 0.002-0.007 的提升，而 max_visits 从 500 增加到 2000 带来约 0.02-0.026 的提升。这说明 Vamana 图质量足够高（build-beam=64），窄波束也能沿正确方向搜索。1000 是性价比拐点：从 500 到 1000 召回跃升约 0.02，从 1000 到 2000 仅提升约 0.005。系统默认取 beam=8, max_visits=1000。

### 6.3 三大优化消融实验

#### 6.3.1 实验设计
在 SIFT-1M 全量 10,000 条查询规模上，通过逐一关闭各优化模块，测试每项优化的独立贡献。基线为"串行 + 无缓存 + 无预取"，逐步叠加缓存（200MB）、多线程（4 线程）及两者组合。搜索参数固定 beam=8, max_visits=1000。

#### 6.3.2 实验结果

| 编号 | 配置 | 耗时 | Recall@10 | 相对基线加速 | 缓存命中率 |
|------|------|------|-----------|-------------|-----------|
| A | 串行，无缓存，无预取 | 待实测 | 0.9897 | 1.0×（基线） | — |
| B | 串行，缓存 200MB | 待实测 | 0.9897 | 待计算 | 待实测 |
| C | 4 线程，无缓存 | 待实测 | 0.9897 | 待计算 | — |
| D | 4 线程，缓存 200MB | 待实测 | 0.9897 | 待计算 | 待实测 |

#### 6.3.3 结果分析（待补充）
预实验初步结论：(1) 缓存命中率约 50%，将约一半磁盘读取转为内存访问；(2) 4 线程约 1.3-1.6× 加速，8 线程不再提升（SSD 随机读带宽瓶颈）；(3) 两项优化叠加接近乘法叠加，Recall 完全不受影响。

### 6.4 缓存容量对比实验

#### 6.4.1 实验设计
在 SIFT-1M 上测试不同缓存大小下的命中率和耗时。取 {0, 2MB, 20MB, 80MB, 200MB}。

#### 6.4.2 实验结果

| 缓存大小 | 命中率 | 耗时 | 相对无缓存加速 |
|----------|--------|------|---------------|
| 0（无缓存） | — | 待实测 | 1.0× |
| 2 MB | 待实测 | 待实测 | 待计算 |
| 20 MB | 2.9% | 待实测 | 待计算 |
| 80 MB | 13.3% | 待实测 | 待计算 |
| 200 MB | 49.8% | 待实测 | 待计算 |

#### 6.4.3 结果分析
命中率随容量非线性增长——小缓存仅覆盖少量热节点，200MB 时可容纳约 32 万条记录，配合图搜索对枢纽节点的重复访问模式命中率达 50%。边际收益随容量递减。

### 6.5 多线程可扩展性测试

#### 6.5.1 实验设计
线程数取 {1, 2, 4, 8, 16}，测量总耗时、QPS 和平均延迟。

#### 6.5.2 实验结果（待实测）

| 线程数 | 总耗时 | QPS | 平均延迟 | 相对串行加速 |
|--------|--------|-----|----------|-------------|
| 1（串行） | 待实测 | 待计算 | 待计算 | 1.0× |
| 2 | 待实测 | 待计算 | 待计算 | 待计算 |
| 4 | 待实测 | 待计算 | 待计算 | 待计算 |
| 8 | 待实测 | 待计算 | 待计算 | 待计算 |
| 16 | 待实测 | 待计算 | 待计算 | 待计算 |

#### 6.5.3 结果分析（待补充）
预实验显示 4 线程约 1.6× 加速，8 线程反而略降。瓶颈在 SSD 随机读带宽——多线程同时 seek 导致磁盘队列深度饱和。建议绘制加速比曲线并标注理想线性参考线。

### 6.6 写入性能与 Compaction 测试

#### 6.6.1 实验设计
分批插入 SIFT-1B queries.bvecs（10,000 条 uint8 向量），flush 取 {0（全部）, 1000, 5000}，监测写入耗时、段数量、Compaction 触发次数和合并耗时。

#### 6.6.2 实验结果（待实测）

| flush 大小 | 写入耗时 | 产生段数 | 合并次数 | 合并耗时 | 写入吞吐量 |
|-----------|----------|---------|---------|---------|-----------|
| 0（全部） | 待实测 | 1 | 0 | — | 待计算 |
| 1000 | 待实测 | 待实测 | 待实测 | 待实测 | 待计算 |
| 5000 | 待实测 | 待实测 | 待实测 | 待实测 | 待计算 |

#### 6.6.3 结果分析（待补充）
预期：flush=0 时写入为顺序追加写最快但内存占用大；flush 较小则触发更多 Compaction，总写入量增加但段数量受控，查询性能更稳定。

### 6.7 读写混合负载测试

#### 6.7.1 实验设计
模拟 Agent 记忆场景：N 个查询线程持续 Top-10 检索，M 个写入线程插入新向量，运行固定时长，监测查询 P50/P95/P99 延迟、写入吞吐量及 Recall 稳定性。

#### 6.7.2 实验结果（待实测）

| 负载类型 | 查询线程 | 写入线程 | 查询QPS | P95延迟 | 写入吞吐 | Recall@10 |
|----------|---------|---------|---------|---------|---------|-----------|
| 纯读 | 4 | 0 | 待实测 | 待实测 | — | 待实测 |
| 读为主 | 4 | 1 | 待实测 | 待实测 | 待实测 | 待实测 |
| 读写均衡 | 2 | 2 | 待实测 | 待实测 | 待实测 | 待实测 |
| 写为主 | 1 | 4 | 待实测 | 待实测 | 待实测 | 待实测 |

#### 6.7.3 结果分析（待补充）
赛题核心评估场景。预期展示 LSM-Tree 批量写入不阻塞查询、Compaction 后台执行不影响前台延迟、Recall 在写入过程中保持稳定。

### 6.8 与赛题要求的对标

| 赛题指标 | 要求 | 本文实现 | 达标 |
|----------|------|---------|------|
| Recall@10 | ≥ 85% | 0.9897（SIFT-1M 全量） | ✓ 远超 |
| 内存占用 | 数据集 10%-20% | 缓存可控（watermark 机制） | 待验证 |
| 读优化-缓存 | 用户态缓存池 + 淘汰策略 | 加权淘汰（priority×1000 + access_count） | ✓ |
| 读优化-预取 | 拓扑感知 + 异步 I/O | PrefetchScheduler + 文件句柄缓存 | ✓ |
| 写优化 | LSM-Tree 随机写转顺序写 | MemTable → Segment Flush → Compaction | ✓ |
| 多核支持 | 并行查询 | ThreadPool + 线程安全 SegmentReader | ✓ |

### 6.9 本章小结
本章通过受控实验量化评估了 vindex 的性能特征。参数敏感性分析确定 beam=8, max_visits=1000 为最佳平衡点。消融实验验证了缓存和并行化的独立贡献。可扩展性测试揭示 SSD 随机读带宽是当前瓶颈。写入路径的 LSM-Tree 策略有效将随机写转为顺序写。整体而言 vindex 在各项指标上满足或超越赛题要求。

---

## 总 结

- **主要成果**：实现了完整的 I/O 优化向量检索存储引擎，覆盖构建、查询、评估、插入全流程
- **核心创新**：加权缓存淘汰 + 图拓扑预取 + LSM 写优化 + PQ 压缩的深度整合
- **性能**：SIFT-1M Recall@10 = 0.9897（远超赛题 85% 要求），200MB 缓存下 50% 命中率
- **工程亮点**：Vamana 性能优化、多线程安全 IO、跨平台兼容（Windows/Linux）、CLI 易用性
- **不足与展望**：io_uring 后端未充分测试、缓存淘汰缺少时间衰减、图动态更新（增量建图）可进一步优化

---

## 参考文献

1. Subramanya, S. J., et al. "DiskANN: Fast accurate billion-point nearest neighbor search on a single node." NeurIPS 2019.
2. Wang, J., et al. "Milvus: A purpose-built vector data management system." SIGMOD 2021.
3. Malkov, Y. A., Yashunin, D. A. "Efficient and robust approximate nearest neighbor search using Hierarchical Navigable Small World graphs." TPAMI 2018.
4. Jegou, H., Douze, M., Schmid, C. "Product quantization for nearest neighbor search." TPAMI 2011.
5. O'Neil, P., et al. "The log-structured merge-tree (LSM-tree)." Acta Informatica 1996.
6. Axboe, J. "Linux io_uring." https://kernel.dk/io_uring.pdf
7. SIFT 数据集. http://corpus-texmex.irisa.fr/
8. DiskANN 开源实现. https://github.com/microsoft/DiskANN


## 使用手册

### 编译
```bash
mkdir build && cd build
cmake .. -G Ninja
cmake --build .
```

### 快速开始

```bash
# 查看帮助
./vindex
./vindex build --help

# 训练 PQ 码本
./vindex train-pq --dataset siftsmall --output siftsmall/codebook.pcb

# 构建索引
./vindex build --dataset siftsmall --output data_out --codebook siftsmall/codebook.pcb

# 查询
./vindex query --dataset siftsmall-pq --topk 10

# 精度评估
./vindex eval --dataset siftsmall-pq --topk 10

# 增量插入
./vindex insert --dataset siftsmall-pq --input new_vectors.fvecs --output data_out

# SIFT-1M 全量评测（推荐参数）
./vindex eval --dataset sift1m-pq --topk 10 --beam 4 --max-visits 1000 --cache 200 --threads 4
```

### 参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--dataset` | — | 数据集名称（siftsmall / siftsmall-pq / sift1m / sift1m-pq） |
| `--beam` | 8 | 束搜索宽度 |
| `--max-visits` | 1000 | 每次搜索最大访问节点数 |
| `--threads` | 4 | 并行线程数 |
| `--cache` | 0 | 缓存大小（MB） |
| `--prefetch` | — | 启用异步预取 |

### 数据集 profile（datasets.json）
```json
{
  "siftsmall-pq.manifest": "siftsmall/pq_data/manifest.txt",
  "siftsmall-pq.query": "siftsmall/siftsmall_query.fvecs",
  "siftsmall-pq.groundtruth": "siftsmall/siftsmall_groundtruth.ivecs",
  "sift1m-pq.manifest": "data_sift1m/manifest.txt",
  "sift1m-pq.query": "SIFT-1M/sift_query.fvecs",
  "sift1m-pq.groundtruth": "SIFT-1M/sift_groundtruth.ivecs"
}
```

### 磁盘文件
- `*.vsg`：段文件（向量数据 + 图邻居 + PQ 码）
- `manifest.txt`：段清单（路径 | 起始ID | 层级）
- `*.pcb`：PQ 码本文件
- `datasets.json`：数据集路径配置文件
