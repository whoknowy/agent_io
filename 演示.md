# 训练PQ码本 + 构建small索引
./build-uring/vindex train-pq --dataset siftsmall --output siftsmall/codebook.pcb

./build-uring/vindex build --dataset siftsmall --output data_siftsmall_pq --codebook siftsmall/codebook.pcb

# query
./build-uring/vindex query --dataset sift1m-pq --topk 10 --beam 4 --max-visits 1000


# 插入
./build-uring/vindex insert --dataset sift1m-pq --input SIFT-1M/queries.bvecs \
    --output data_demo --flush 1000

# 1M验证(多线程)
./build-uring/vindex eval --dataset sift1m-pq --beam 8 --max-visits 1000 --threads 4 --limit 10000

# 预取
./build-uring/vindex eval --dataset sift1m-pq --thread 4 --limit 10000 --prefetch

# 缓存
./build-uring/vindex eval --dataset sift1m-pq --thread 4 --limit 10000 --cache

# 压测
./build-uring/vindex stress --dataset sift1m-pq --write-input SIFT-1M/queries.bvecs --read-threads 4 --write-threads 1 --duration 30


